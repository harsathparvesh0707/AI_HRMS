# AI_HRMS

## Quick Start with Docker

```bash
# Build and run
docker build -t ai-hrms .
docker run -p 8000:8000 ai-hrms
```

Access at: http://localhost:8000

## Manual Setup

```bash
# Install Python 3.8+
pip install -r requirements.txt
python AIHRMS/hrms_ai/run.py
```